{
  "name": "moneypulse",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@10.22.0",
  "scripts": {
    "build": "pnpm -r build",
    "dev:web": "pnpm --filter @moneypulse/web dev",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck"
  }
}

